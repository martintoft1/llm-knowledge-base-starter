# Claude Instructions

Read and follow `AGENTS.md` as the shared entry point.

Use the same OKF v0.2 conformance rules, progressive autonomy boundary, raw-file immutability, and approval requirements. Claude-specific tools or memory must not bypass them.

Do not define a separate schema here. The pinned specification and local rules named in `AGENTS.md` remain authoritative.
